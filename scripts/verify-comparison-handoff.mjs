import {chromium} from 'playwright';import assert from 'node:assert/strict';
const browser=await chromium.launch({headless:true});const page=await browser.newPage();
try{await page.goto(process.env.TEST_URL||'http://localhost:3107/sase/home/');await page.getByRole('button',{name:'Compare',exact:true}).click();await page.getByRole('heading',{name:'Compare SASE & SD-WAN providers',exact:true}).waitFor();console.log((await page.locator('.nf-buying-research').innerText()).slice(0,1500));await page.locator('.nf-buying-research textarea').first().fill('We manufacture equipment at five UK sites and need resilient managed SASE.');await page.getByRole('button',{name:'Find providers for my project',exact:true}).first().click();await page.getByRole('dialog').waitFor();assert((await page.getByLabel('What do you need to achieve?',{exact:true}).inputValue()).includes('manufacture equipment'));console.log('PASS public comparison opens and carries requirement into same mounted project review');}finally{await browser.close();}
