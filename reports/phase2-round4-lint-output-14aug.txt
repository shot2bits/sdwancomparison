
/tmp/sdwan_reliability_gate/src/components/ProjectDesk.tsx
  1008:32  error  Error: Calling setState synchronously within an effect can trigger cascading renders

Effects are intended to synchronize state between React and external systems such as manually updating the DOM, state management libraries, or other platform APIs. In general, the body of an effect should do one or both of the following:
* Update external systems with the latest state from React.
* Subscribe for updates from some external system, calling setState in a callback function when external state changes.

Calling setState synchronously within an effect body causes cascading renders that can hurt performance, and is not recommended. (https://react.dev/learn/you-might-not-need-an-effect).

/tmp/sdwan_reliability_gate/src/components/ProjectDesk.tsx:1008:32
  1006 |
  1007 |     const p = new URLSearchParams(window.location.search);
> 1008 |     if (p.get("test") === "1") setTestMode(true);
       |                                ^^^^^^^^^^^ Avoid calling setState() directly within an effect
  1009 |     const scopeParam = p.get("scope");
  1010 |     const seedFacts: FieldUpdate[] = [];
  1011 |     if (scopeParam && ["security", "managed_security", "sase", "sdwan", "sse"].includes(scopeParam)) {                                                                                                                                                                                    react-hooks/set-state-in-effect
  1280:7   error  Error: Calling setState synchronously within an effect can trigger cascading renders

Effects are intended to synchronize state between React and external systems such as manually updating the DOM, state management libraries, or other platform APIs. In general, the body of an effect should do one or both of the following:
* Update external systems with the latest state from React.
* Subscribe for updates from some external system, calling setState in a callback function when external state changes.

Calling setState synchronously within an effect body causes cascading renders that can hurt performance, and is not recommended. (https://react.dev/learn/you-might-not-need-an-effect).

/tmp/sdwan_reliability_gate/src/components/ProjectDesk.tsx:1280:7
  1278 |   useEffect(() => {
  1279 |     if (!securityScope || live.length === 0) {
> 1280 |       setVerdict(null);
       |       ^^^^^^^^^^ Avoid calling setState() directly within an effect
  1281 |       return;
  1282 |     }
  1283 |     let cancelled = false;                                                                                                                                                                                                                                                                                                                                                                         react-hooks/set-state-in-effect
  1368:5   error  Error: Calling setState synchronously within an effect can trigger cascading renders

Effects are intended to synchronize state between React and external systems such as manually updating the DOM, state management libraries, or other platform APIs. In general, the body of an effect should do one or both of the following:
* Update external systems with the latest state from React.
* Subscribe for updates from some external system, calling setState in a callback function when external state changes.

Calling setState synchronously within an effect body causes cascading renders that can hurt performance, and is not recommended. (https://react.dev/learn/you-might-not-need-an-effect).

/tmp/sdwan_reliability_gate/src/components/ProjectDesk.tsx:1368:5
  1366 |     /* The change carries the marker AND one thread line (round 6): the
  1367 |        rules are already written in when the line appears. */
> 1368 |     setChangedSlots((prev) => [...new Set([...prev, ...merged.changed.map((id) => `rule:${id}`)])]);
       |     ^^^^^^^^^^^^^^^ Avoid calling setState() directly within an effect
  1369 |     const n = merged.changed.length;
  1370 |     say(`Your sector writes ${numWord(n)} rule${n === 1 ? "" : "s"} into the statement; they are in already, each with its reason, and any one can be dropped.`);
  1371 |     // eslint-disable-next-line react-hooks/exhaustive-deps  react-hooks/set-state-in-effect
  2306:59  error  Error: Calling setState synchronously within an effect can trigger cascading renders

Effects are intended to synchronize state between React and external systems such as manually updating the DOM, state management libraries, or other platform APIs. In general, the body of an effect should do one or both of the following:
* Update external systems with the latest state from React.
* Subscribe for updates from some external system, calling setState in a callback function when external state changes.

Calling setState synchronously within an effect body causes cascading renders that can hurt performance, and is not recommended. (https://react.dev/learn/you-might-not-need-an-effect).

/tmp/sdwan_reliability_gate/src/components/ProjectDesk.tsx:2306:59
  2304 |   useEffect(() => {
  2305 |     const w = window as unknown as { SpeechRecognition?: unknown; webkitSpeechRecognition?: unknown };
> 2306 |     if (w.SpeechRecognition || w.webkitSpeechRecognition) setVoiceSupported(true);
       |                                                           ^^^^^^^^^^^^^^^^^ Avoid calling setState() directly within an effect
  2307 |     return () => { try { voiceRec.current?.stop(); } catch { /* gone */ } };
  2308 |   }, []);
  2309 |   const startVoice = () => {                                                                                                                     react-hooks/set-state-in-effect

✖ 4 problems (4 errors, 0 warnings)

